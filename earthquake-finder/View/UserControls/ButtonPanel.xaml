<UserControl x:Class="earthquake_finder.View.UserControls.ButtonPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:userControls="clr-namespace:earthquake_finder.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions >
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="userControls:FilterButton">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>

        <userControls:FilterButton Grid.Column="0" Label="Past hour"/>
        <userControls:FilterButton Grid.Column="1" Label="Past day"/>
        <userControls:FilterButton Grid.Column="2" Label="Past week"/>
        <userControls:FilterButton Grid.Column="3" Label="Past month"/>
    </Grid>
</UserControl>
