<UserControl x:Class="earthquake_finder.View.UserControls.ButtonPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:userControls="clr-namespace:earthquake_finder.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions >
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Grid">
                <Setter Property="Margin" Value="10"/>
            </Style>
        </Grid.Resources>

        <Button Grid.Column="0" Content="Past hour" Style="{StaticResource SelectedButton}"/>
        <Button Grid.Column="1" Content="Past day"/>
        <Button Grid.Column="2" Content="Past week"/>
        <Button Grid.Column="3" Content="Past month"/>
    </Grid>
</UserControl>
